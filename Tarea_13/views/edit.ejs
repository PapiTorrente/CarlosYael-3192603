<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/estilos.css">
    <title>Editar Usuario</title>
</head>
<body>
    <h1>Editar Usuario</h1>
    <form action="/update/<%= usuarios.id_usr %>" method="POST">
        <div class="formulario">
            <div class="item">
                <label for="name">Nombre:</label>
                <input type="text" id="name" name="nombre" value="<%= usuarios.nombre %>" required maxlength="20" pattern="[a-zA-Z ]+">
            </div>
            <div class="item">
                <label for="ap_paterno">Apellido Paterno:</label>
                <input type="text" id="ap_paterno" name="ap_paterno" value="<%= usuarios.ap_paterno %>" required maxlength="20" pattern="[a-zA-Z ]+">
            </div>
            <div class="item">
                <label for="ap_materno">Apellido Materno:</label>
                <input type="text" id="ap_materno" name="ap_materno" value="<%= usuarios.ap_materno %>" required maxlength="20" pattern="[a-zA-Z ]+">
            </div>
            <div class="item">
                <label for="edad">Edad:</label>
            <input type="number" id="edad" name="edad" value="<%= usuarios.edad %>" required maxlength="3" pattern="\d{1,3}">
            </div>
        </div>
        <div class="formulario">
            <div class="item">
                <label for="oficio">Oficio:</label>
                <input type="text" id="oficio" name="oficio" value="<%= usuarios.oficio %>" required maxlength="20" pattern="[a-zA-Z ]+">
            </div>
            <div class="item">
                <label for="email">Correo:</label>
                <input type="email" id="email" name="correo" value="<%= usuarios.correo %>" required maxlength="40">
            </div>
            <div class="item">
                <label for="telefono">Telefono:</label>
                <input type="number" id="telefono" name="telefono" value="<%= usuarios.telefono %>" required maxlength="10" pattern="\d{1,10}">
            </div>
            <div class="item">
                <label for="direccion">Dirección:</label>
            <input type="text" id="direccion" name="direccion" value="<%= usuarios.direccion %>" required maxlength="50" pattern="[a-zA-Z ]+">
            </div>
        </div>
        <button type="submit">Actualizar</button>
    </form>

    <div class="boton">
        <button><a href="/">Regresar sin Actualizar</a></button>
    </div>
</body>
</html>